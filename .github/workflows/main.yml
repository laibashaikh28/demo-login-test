name: Login Demo

on:
  push:
    branches: 
      - master
  workflow_dispatch:

jobs:
  specflow:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '3.1'
          
      - name: Restore dependencies
        run: dotnet restore
          
      - name: Build solution
        run: dotnet build --configuration Release
   
      - name: Run tests
        run: dotnet test --logger "trx;LogFileName=TestResults.html"
        
      - name: Publish
        run: dotnet publish 

      - name: Upload Build Artifact
        uses: actions/upload-artifact@v2
        with:
          name: Specflow_Artifact
          path: /home/runner/work/demo-login-test/demo-login-test/LoginDemo/bin/Debug/netcoreapp3.1/LoginDemo.dll
          
      - name: Configure Git
        run: |
          git config --global user.email "laibaovais05@gmail.com"
          git config --global user.name "laibashaikh28"
          git add -A
          git commit -m "commit message"
          git push origin master
          git tag -a 1.0.1 -m "Initial realease version 1.0.1"
          git push origin 1.0.1

      - name: Checkout Release Repo
        uses: actions/checkout@v3
        with:
          repository: laibashaikh28/release
          token: ${{ secrets.ACTIONS_TOKEN }}
          path: target

      - name: Copy files to release
        run: |
          rm -r ${{ github.workspace }}/target/demo-login-test
          mkdir -p target/URBE
          cp -r ${{ github.workspace }}/demo-login-test/artifacts ${{ github.workspace }}/target/demo-login-test

      - name: Commit changes to release
        run: |
          cd target
          git config --global user.email "laibaovais05@gmail.com"
          git config --global user.name "laibashaikh28"
          git add .
          git commit -m "Add newly generated artifacts and config files." || true
          git push origin main --force

      # - name: Install surge
      #   run: npm install --global surge
      # - name: Copy html report surge default html file
      #   run: sudo cp ./LoginDemo/TestResults/TestResults.html ./LoginDemo/TestResults/200.html
      # - name: Deploy using surge
      #   run: surge ./LoginDemo/TestResults testsreport.surge.sh --token ${{ secrets.SURGE_KEY }}
